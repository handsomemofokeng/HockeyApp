<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.hockeyapp">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".ApplicationClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterUserActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SetRolesActivity" />
        <activity android:name=".AddTeamsActivity" />
        <activity android:name=".TeamListsActivity" />
        <activity
            android:name=".AddPlayerActivity"
            android:label="@string/title_activity_register_player"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MovePlayersActivity" />
        <activity
            android:name=".MatchStatsActivity"
            android:label="@string/title_activity_match_stats" />
        <!-- <activity -->
        <!-- android:name=".TeamsActivity" -->
        <!-- android:label="@string/title_activity_teams" -->
        <!-- android:theme="@style/AppTheme.NoActionBar" /> -->
        <activity android:name=".MyPlayersActivity" />
        <activity android:name=".PlayerDetailsActivity" />
        <activity android:name=".MatchesMenuActivity" />
        <activity android:name=".MatchLineUpActivity" />
        <activity android:name=".MatchListActivity"></activity>
    </application>

</manifest>